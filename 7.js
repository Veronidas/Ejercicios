//Cadena a convertir
let name = "veronika";
//iteracion a la cadena
for (i = 0; i < name.length; i++) {
//imprimimos en la consola el nombre, haciendo que imprima por separado cada iteracion y le aplicamos el metodo toUpperCase para que este todo en mayusculas
  console.log(name[i].toUpperCase());
  }